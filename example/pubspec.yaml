name: example
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.4 <3.0.0'

# dependencies:
#   cinch:
#     path: ../cinch

dependency_overrides:
  cinch:
    path: ../cinch

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  build_runner: ^2.0.6
  cinch_gen:
    path: ../cinch_gen
