name: example
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

dependency_overrides:
  cinch:
    path: ../packages/cinch

dev_dependencies:
  freezed: ^3.2.5
  json_serializable: ^6.13.0
  lints: ^6.1.0
  test: ^1.31.0
  build_runner: ^2.13.0
  cinch_gen:
    path: ../packages/cinch_gen